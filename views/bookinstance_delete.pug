extends layout

block content 
    h1 #{title}: #{book_instance._id}

    p Do you really want to delete this Bookinstance?

    form(method='POST')
      div.form-group
        input#authorid.form-control(type='hidden', name='bookinstanceid', value=book_instance._id )

      button.btn.btn-primary(type='submit') Delete